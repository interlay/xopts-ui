import React, { ReactElement } from "react";
import Page from "../page/page";

export default function TradeOptionsPage (): ReactElement {
    return <Page>
        <div>some table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options
        some table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with optionssome table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with optionssome table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options
        some table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with optionssome table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options
        some table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with optionssome table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options
        some table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with optionssome table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options
        some table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with optionssome table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options table with options some table with options
        </div>
    </Page>;
}